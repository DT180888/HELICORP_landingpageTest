import React from 'react';

/**
 * Hero section component placeholder.
 * Will be fully implemented in Giai đoạn 3A.
 * 
 * @returns {JSX.Element} The rendered Hero placeholder.
 */
export const Hero = React.memo(function Hero() {
  return (
    <section className="py-24 text-center">
      <h2 className="text-3xl font-semibold text-zinc-300">Hero Section (Placeholder)</h2>
    </section>
  );
});
