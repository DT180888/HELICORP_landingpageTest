import React from 'react';

/**
 * SubscribeForm section component placeholder.
 * Will be fully implemented in Giai đoạn 4.
 * 
 * @returns {JSX.Element} The rendered SubscribeForm placeholder.
 */
export const SubscribeForm = React.memo(function SubscribeForm() {
  return (
    <section className="py-24 text-center bg-zinc-950/30">
      <h2 className="text-3xl font-semibold text-zinc-300">Subscribe Form Section (Placeholder)</h2>
    </section>
  );
});
