import React from 'react';

/**
 * Navbar component placeholder.
 * Will be fully implemented in Step 2.2.
 * 
 * @returns {JSX.Element} The rendered Navbar placeholder.
 */
export const Navbar = React.memo(function Navbar() {
  return (
    <header className="w-full py-4 text-center border-b border-zinc-800/50 bg-oled-black">
      <div className="text-xl font-bold tracking-tight text-zinc-400">Navbar (Placeholder)</div>
    </header>
  );
});
